---
import { DEFAULT_CAROUSEL_IMAGES } from '../lib/cms/defaults'

interface Props {
  images?: string[];
}

const { images = [] } = Astro.props;
const sliderImages = images.length ? images : DEFAULT_CAROUSEL_IMAGES;
---

<div class="slider-wrap">
  <div class="my-slider">
    {sliderImages.map((src) => (
      <div>
        <img src={src} alt="Kita impression" loading="lazy" class="w-full object-cover rounded-xl" />
      </div>
    ))}
  </div>
  <div class="slider-controls" aria-label="Bildnavigation">
    <button type="button" class="slider-arrow" data-controls="prev" aria-label="Vorheriges Bild">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" width="20" height="20" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5 8.25 12l7.5-7.5" /></svg>
    </button>
    <button type="button" class="slider-arrow" data-controls="next" aria-label="Nächstes Bild">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" width="20" height="20" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="m8.25 4.5 7.5 7.5-7.5 7.5" /></svg>
    </button>
  </div>
  <div class="slider-dots" aria-label="Bildauswahl"></div>
</div>

<style is:global>
  .slider-wrap {
    position: relative;
    border-radius: var(--radius-lg);
  }

  .my-slider {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
  }

  .my-slider img {
    height: clamp(300px, 42vw, 475px);
  }

  .slider-controls {
    position: absolute;
    top: 50%;
    left: 0;
    right: 0;
    transform: translateY(-50%);
    display: flex;
    justify-content: space-between;
    pointer-events: none;
    z-index: 10;
    padding: 0 12px;
  }

  .slider-arrow {
    pointer-events: auto;
    background: rgba(255, 255, 255, 0.95);
    border: 1px solid var(--border);
    box-shadow: var(--shadow-md);
    border-radius: 999px;
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
    color: var(--ink-700);
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s, background 0.2s, color 0.2s;
  }

  .slider-arrow:hover {
    transform: scale(1.08);
    box-shadow: var(--shadow-lg);
    background: var(--brand-50);
    color: var(--brand-700);
  }

  .slider-dots {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 8px;
    margin-top: 14px;
  }

  .slider-dot {
    width: 10px;
    height: 10px;
    border: 0;
    border-radius: 999px;
    background: #c4c9cf;
    cursor: pointer;
    padding: 0;
    transition: transform 0.15s ease, background-color 0.2s ease, width 0.2s ease;
  }

  .slider-dot:hover {
    transform: scale(1.15);
    background: #9ca3af;
  }

  .slider-dot.active {
    background: var(--brand-600);
    width: 24px;
  }
</style>

<script>
  // Simple vanilla JS slider
  document.addEventListener('astro:page-load', () => {
    document.querySelectorAll('.slider-wrap').forEach((wrapper) => {
      const slider = wrapper.querySelector('.my-slider');
      if (!slider) return;

      const slides = slider.children;
      const totalSlides = slides.length;
      let currentSlide = 0;
      let autoplayInterval: ReturnType<typeof setInterval>;

      const dotsContainer = wrapper.querySelector('.slider-dots');
      const dots: HTMLButtonElement[] = [];
      if (dotsContainer) {
        for (let i = 0; i < totalSlides; i++) {
          const dot = document.createElement('button');
          dot.type = 'button';
          dot.className = 'slider-dot';
          dot.setAttribute('aria-label', `Zu Bild ${i + 1}`);
          dotsContainer.appendChild(dot);
          dots.push(dot);
        }
      }

      function updateDots(index: number) {
        dots.forEach((dot, i) => dot.classList.toggle('active', i === index));
      }

      function showSlide(index: number) {
        Array.from(slides).forEach((slide, i) => {
          (slide as HTMLElement).style.display = i === index ? 'block' : 'none';
        });
        updateDots(index);
      }

      function nextSlide() {
        currentSlide = (currentSlide + 1) % totalSlides;
        showSlide(currentSlide);
      }

      function prevSlide() {
        currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
        showSlide(currentSlide);
      }

      function startAutoplay() {
        autoplayInterval = setInterval(nextSlide, 5000);
      }

      function stopAutoplay() {
        clearInterval(autoplayInterval);
      }

      // Initialize
      showSlide(0);
      startAutoplay();

      // Controls
      const prevBtn = wrapper.querySelector('[data-controls="prev"]');
      const nextBtn = wrapper.querySelector('[data-controls="next"]');

      prevBtn?.addEventListener('click', () => {
        stopAutoplay();
        prevSlide();
        startAutoplay();
      });

      nextBtn?.addEventListener('click', () => {
        stopAutoplay();
        nextSlide();
        startAutoplay();
      });

      dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
          stopAutoplay();
          currentSlide = index;
          showSlide(currentSlide);
          startAutoplay();
        });
      });
    });
  });
</script>
